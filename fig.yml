galweb:
  build: crane/docker/web_server
  links:
  - galdb:galdb
  - galcache:galcache
  ports:
  - "80:80"
  - "443:443"
  volumes:
  - ./:/opt/www

galdb:
  build: crane/docker/db_server
  ports:
  - "3306:3306"

galcache:
  build: crane/docker/cache_server
